/**
 * 
 */
//댓글 작성하기 버튼 클릭 시, commentSubmit 함수 실행, comment 테이블에 insert
let submitButtonClick = document.getElementById("");

submitButtonClick.addEventListener("commentSubmit", click);

function commentSubmit() {
    alert("버튼이 클릭되었습니다.");
}